<!DOCTYPE html>
<html lang="es">
    <head>
    <title>Kuracisto</title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="turbo-visit-control" content="reload">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <!-- javascript_importmap_tags eliminado para evitar error -->
  <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true, type: "module" %>
  </head>

  <body class="bg-gray-50 min-h-screen flex flex-col">
    <!-- Barra de navegación - Estilo moderno con Tailwind -->
    <nav class="bg-blue-800 shadow-lg">
      <div class="container mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <!-- Logo / Título -->
          <div class="flex items-center">
            <%= link_to root_path, class: "text-white text-xl font-bold hover:text-blue-200 transition" do %>
              Kuracisto
            <% end %>
          </div>

          <!-- Menú en pantallas grandes -->
          <div class="hidden md:flex items-center space-x-4">
            <% if user_signed_in? %>
              <%= link_to "Mi Agenda", pacientes_agenda_path, class: "text-white hover:text-blue-200 px-3 py-2 rounded-md text-sm font-medium transition" %>

              <% if current_user.medico? %>
                <%= link_to "Dashboard Médico", medicos_dashboard_path, class: "text-white hover:text-blue-200 px-3 py-2 rounded-md text-sm font-medium transition" %>
              <% end %>

              <% if current_user.empleado? %>
                <%= link_to "Dashboard Empleado", empleados_dashboard_path, class: "text-white hover:text-blue-200 px-3 py-2 rounded-md text-sm font-medium transition" %>
              <% end %>

        <%= button_to "Cerrar sesión", destroy_user_session_path,
          method: :delete,
          class: "bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium transition border-none",
          form_class: "inline" %>

            <% else %>
              <%= link_to "Iniciar sesión", new_user_session_path, class: "text-white hover:text-blue-200 px-3 py-2 rounded-md text-sm font-medium transition" %>
              <%= link_to "Registrarse", new_user_registration_path, class: "bg-blue-600 hover:bg-blue-700 text-white px-4 py-2 rounded-md text-sm font-medium transition" %>
            <% end %>
          </div>

          <!-- Botón hamburguesa (móvil) -->
          <div class="md:hidden flex items-center">
            <button id="mobile-menu-button" class="text-white focus:outline-none">
              <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
              </svg>
            </button>
          </div>
        </div>
      </div>

      <!-- Menú móvil oculto -->
      <div id="mobile-menu" class="md:hidden bg-blue-900 hidden shadow-lg">
        <div class="px-2 pt-2 pb-3 space-y-1">
          <% if user_signed_in? %>
            <%= link_to "Mi Agenda", pacientes_agenda_path, class: "text-white hover:bg-blue-700 block px-3 py-2 rounded-md text-base font-medium" %>

            <% if current_user.medico? %>
              <%= link_to "Dashboard Médico", medicos_dashboard_path, class: "text-white hover:bg-blue-700 block px-3 py-2 rounded-md text-base font-medium" %>
            <% end %>

            <% if current_user.empleado? %>
              <%= link_to "Dashboard Empleado", empleados_dashboard_path, class: "text-white hover:bg-blue-700 block px-3 py-2 rounded-md text-base font-medium" %>
            <% end %>

      <%= button_to "Cerrar sesión", destroy_user_session_path,
        method: :delete,
        class: "text-white hover:bg-red-700 block px-3 py-2 rounded-md text-base font-medium border-none",
        form_class: "block" %>

          <% else %>
            <%= link_to "Iniciar sesión", new_user_session_path, class: "text-white hover:bg-blue-700 block px-3 py-2 rounded-md text-base font-medium" %>
            <%= link_to "Registrarse", new_user_registration_path, class: "bg-blue-600 hover:bg-blue-700 text-white block px-3 py-2 rounded-md text-base font-medium" %>
          <% end %>
        </div>
      </div>
    </nav>

    <!-- Contenido principal -->
    <main class="container mx-auto px-4 sm:px-6 lg:px-8 py-8">
      <% if notice %>
        <div class="bg-green-100 text-green-800 p-4 rounded mb-6"><%= notice %></div>
      <% end %>
      <% if alert %>
        <div class="bg-red-100 text-red-800 p-4 rounded mb-6"><%= alert %></div>
      <% end %>

      <%= yield %>
    </main>

    <!-- Pie de página -->
    <footer class="bg-white border-t border-gray-200 py-6 mt-auto">
      <div class="container mx-auto px-4 text-center text-gray-500 text-sm">
        © 2025 Kuracisto — Sistema de gestión médica
      </div>
    </footer>

    <!-- Script para menú móvil (sin Bootstrap JS) -->
    <script>
      document.getElementById('mobile-menu-button').addEventListener('click', () => {
        const menu = document.getElementById('mobile-menu');
        menu.classList.toggle('hidden');
      });
    </script>
  </body>
</html>